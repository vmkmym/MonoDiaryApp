<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/hhh"
        android:label="@string/app_name"
        android:roundIcon="@drawable/hhh"
        android:supportsRtl="true"
        android:theme="@style/Theme.MonoDiaryApp"
        tools:targetApi="31">
        <activity
            android:name=".GalleryActivity"
            android:exported="false"
            android:label="@string/title_activity_gallery"
            android:theme="@style/Theme.MonoDiaryApp" />
        <activity
            android:name=".AddDiaryActivity"
            android:exported="false"
            android:label="AddDiaryActivity"
            android:theme="@style/Theme.MonoDiaryApp" />
        <activity
            android:name=".EditDiaryActivity"
            android:exported="false"
            android:label="EditDiaryActivity"
            android:theme="@style/Theme.MonoDiaryApp" />
        <activity
            android:name=".HomeActivity"
            android:exported="false"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.MonoDiaryApp" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MonoDiaryApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>